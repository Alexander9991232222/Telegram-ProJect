# 🚀 Plan Hero Bot

Telegram-бот для управління завданнями, розкладом та нагадуваннями. Допомагає структурувати день і не забувати про важливі справи. Побудований на базі **aiogram 3.x**.

## 🛠 Технології
* **Python 3.12+**
* **Aiogram 3.x** — сучасний фреймворк для Telegram-ботів.
* **SQLAlchemy 2.0** — робота з базою даних (ORM).
* **SQLite** — легка та швидка БД в одному файлі.
* **Uv** — надшвидкий менеджер пакетів та середовищ.

---

## 📂 Структура проекту

```text
Telegram-ProJect/
├── db/
│   └── database.db         # Файл бази даних SQLite
├── src/
│   ├── bot/
│   │   ├── handlers/       # Обробники (start.py, main_keyboard_callback.py)
│   │   └── keyboards/      # Клавіатури (main_keyboard.py)
│   ├── database/
│   │   ├── models/         # Моделі БД (base.py, user.py)
│   │   ├── repositories/   # Репозиторії (user_repository.py)
│   │   └── db_manager.py   # Налаштування підключення до БД
│   ├── models/             # (Дублікат, потребує очищення)
│   ├── services/           # Бізнес-логіка бота
│   ├── config.py           # Конфігурація та токен
│   └── main.py             # Точка входу (запуск бота)
├── .env                    # Секретні змінні оточення
├── .gitignore              # Ігноровані файли
├── pyproject.toml          # Залежності проекту (uv)
└── README.md               # Документація проекту
```

## ⚙️ Встановлення та запуск
* **Підготовка**
Переконайтеся, що у вас встановлено uv. Якщо ні, встановіть через PowerShell: powershell -c "irm [https://astral-sh.uv.io/install.ps1](https://astral-sh.uv.io/install.ps1) | iex"

* **Налаштування середовища**
Створіть файл .env у корені проекту та додайте свій токен:BOT_TOKEN=ваш_токен_від_BotFather

* **Запуск бота**
Встановлення залежностей та запуск однією командою: ./run.ps1

## Функціонал
📅 Календар — перегляд завдань та графіку за датами.

➕ Додати завдання — створення нових нотаток у базі даних.

🔔 Нагадування — список активних сповіщень та їх налаштування.

📋 Розклад дня — ваш персональний план справ на сьогодні.

⚙️ Налаштування — конфігурація профілю та сповіщень.

## 🤝 Розробка
Для розширення можливостей бота:

* **Створіть новий роутер у src/bot/handlers/.**

* **Опишіть необхідні кнопки у src/bot/keyboards/user_keyboards.py.**

* **Важливо: Завжди реєструйте новий роутер у src/main.py через dp.include_router().**