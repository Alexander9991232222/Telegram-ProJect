🚀 Plan Hero Bot
Telegram-бот для управління завданнями, розкладом та нагадуваннями. Допомагає структурувати день і не забувати про важливі справи.

🛠 Технології
Python 3.12+

Aiogram 3.x — сучасний фреймворк для Telegram-ботів.

SQLAlchemy 2.0 — робота з базою даних (ORM).

SQLite — легка та швидка БД в одному файлі.

📂 Структура проекту

Telegram-ProJect/
├── src/
│   ├── bot/
│   │   ├── handlers/       # Логіка обробки повідомлень (роутери)
│   │   ├── keyboards/      # Головне меню та кнопки (Inline/Reply)
│   │   └── states/         # Стани FSM (машина станів)
│   ├── database/
│   │   ├── models/         # Опис таблиць бази даних
│   │   └── db_manager.py   # Підключення та управління БД
│   ├── config.py           # Конфігурація (Токен бота, шляхи)
│   └── main.py             # Точка входу в додаток
├── .env                    # Секретні ключі (BOT_TOKEN)
├── pyproject.toml          # Залежності проекту
└── README.md

⚙️ Встановлення та запуск

1. Підготовка
Переконайтеся, що у вас встановлено uv. Якщо ні, встановіть через PowerShell:

commands:
 powershell -c "irm https://astral-sh.uv.io/install.ps1 | iex"

2. Налаштування середовища

# Створіть файл .env у корені проекту та додайте свій токен:
BOT_TOKEN=ваш_токен_від_BotFather

# Встановлення залежностей та запуск однією командою:
.\run.ps1

📋 Функціонал
[x] Реєстрація: Автоматичне збереження користувача в базу при першому запуску (/start).

[x] Головне меню: Зручна Reply-клавіатура з іконками замість стандартної панелі вводу.

[x] Календар: Перегляд завдань за датами.

[ ] Нагадування: Налаштування сповіщень у вказаний час (у розробці).

[ ] Завдання: Додавання, видалення та відмітки про виконання.

🤝 Розробка
Для розширення можливостей бота:

Створіть новий роутер у src/bot/handlers/.

Опишіть необхідні кнопки у src/bot/keyboards/.

Підключіть роутер у файлі src/main.py через dp.include_router().

Іконки меню:
📅 Календар — перегляд графіку.

➕ Додати завдання — швидкий запис справи.

🔔 Нагадування — список активних сповіщень.

📋 Розклад дня — план на сьогодні.

Порада щодо використання:
Щоб оновити структуру бази даних (наприклад, після зміни моделей), видаліть файл database.db і перезапустіть бота — таблиці створяться автоматично завдяки Base.metadata.create_all.